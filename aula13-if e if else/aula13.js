const a = 10
const b = 20
if (a < b) {
    console.log(b + " é maior que " + a)
}

let energia = 100
let clima = "sol"

if (energia > 80 && clima == "sol") {
    console.log("Bom para ir à praia")
} else {
    console.log("Não está tão bom para ir à praia")
}